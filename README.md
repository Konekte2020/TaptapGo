# TapTapGo

## Démarrer le backend

Depuis le dossier **backend** :

- PowerShell : `.\start.ps1`
- CMD : `start.bat`

Si vous voyez *« python n'est pas reconnu »*, Python n'est pas dans le PATH. Voir **backend/README.md** pour corriger ça une fois pour toutes.
