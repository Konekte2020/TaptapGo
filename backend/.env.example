# Copier ce fichier en .env et remplir les valeurs.
# Ne jamais committer .env (déjà dans .gitignore).

# === Obligatoire ===
SUPABASE_URL=https://votre-projet.supabase.co
SUPABASE_KEY=votre_cle_anon_supabase
JWT_SECRET=une_cle_secrete_longue_et_aleatoire

# === Optionnel ===
# Algorithme JWT (défaut: HS256)
# JWT_ALGORITHM=HS256

# CORS: origines autorisées, séparées par des virgules. Mettre * en dev uniquement.
# En production: https://votre-app.com,https://admin.votre-app.com
CORS_ORIGINS=*

# === Build APK White Label (Jenere APK) ===
# Prérequis: ANDROID_HOME ou ANDROID_SDK_ROOT, Node.js 18+, Java 11+, npm

# Build EAS (cloud) et soumission Play Store — obligatoire pour "Build rapid" / EAS Submit
# EXPO_TOKEN=votre_token_expo_dev

# URL de l'API injectée dans les builds (app pointe vers votre backend)
# TAPTAPGO_PUBLIC_BACKEND_URL=https://api.votre-domaine.com
# EXPO_PUBLIC_BACKEND_URL=https://api.votre-domaine.com

# Dossier des builds locaux (Windows: chemin court requis, défaut: C:\tb)
# TAPTAPGO_BUILD_DIR=C:\tb

# SUBST désactivé par défaut (cause "different roots" Z: vs C: avec React Native codegen)
# TAPTAPGO_USE_SUBST=0

# Cache Gradle: par défaut utilise %USERPROFILE%\.gradle (évite metadata.bin corrompu)
# TAPTAPGO_GRADLE_HOME=C:\chemin\gradle

# Chemin du projet frontend (défaut: ../frontend depuis backend)
# TAPTAPGO_BASE_PROJECT_PATH=C:/chemin/vers/frontend

# Bucket Supabase pour stocker les APK (défaut: builds) — créer le bucket dans Supabase
# TAPTAPGO_BUILDS_BUCKET=builds

# Envoi d'emails depuis le modal White-Label (SuperAdmin)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=votre-email@gmail.com
# SMTP_PASS=mot_de_passe_app
# SMTP_FROM=noreply@taptapgoht.com
