# Copier en .env pour le dev local. Pour les builds EAS (preview/production),
# configurer les secrets dans le projet Expo : eas secret:create
# ou dans eas.json (sans committer de vraies clés).

# URL de l'API backend (obligatoire en prod)
EXPO_PUBLIC_BACKEND_URL=http://localhost:8000
EXPO_PUBLIC_BACKEND_URL_ANDROID=http://10.0.2.2:8000
# EXPO_PUBLIC_BACKEND_URL_IOS=
# EXPO_PUBLIC_BACKEND_URL_NATIVE=
# EXPO_PUBLIC_BACKEND_URL_WEB=

# Mapbox (cartes) - créer un token sur mapbox.com
# EXPO_PUBLIC_MAPBOX_TOKEN=

# White-label (optionnel)
# EXPO_PUBLIC_BRAND_ID=
# EXPO_PUBLIC_BRAND_NAME=
