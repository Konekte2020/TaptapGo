# Copier en .env pour le dev local. Tu peux déjà y mettre EXPO_PUBLIC_MAPBOX_TOKEN.

# Pour les builds EAS (preview/production), les variables sont injectées par EAS Secrets.
# Obligatoire en prod (eas secret:create depuis frontend/) :
#   EXPO_PUBLIC_BACKEND_URL, EXPO_PUBLIC_BACKEND_URL_ANDROID, EXPO_PUBLIC_MAPBOX_TOKEN
# Optionnel : EXPO_PUBLIC_BRAND_ID, EXPO_PUBLIC_BRAND_NAME

# URL de l'API backend (dev local ; prod = https://taptapgo.onrender.com)
EXPO_PUBLIC_BACKEND_URL=http://localhost:8000
# Émulateur Android uniquement (10.0.2.2 = PC depuis l'émulateur)
EXPO_PUBLIC_BACKEND_URL_ANDROID=http://10.0.2.2:8000
# Téléphone réel : remplacer par l'IP de ton PC sur le WiFi (ex: http://192.168.1.10:8000)
# Voir avec: ipconfig (Windows) → Adresse IPv4 de la carte WiFi
# EXPO_PUBLIC_BACKEND_URL_ANDROID=http://192.168.1.10:8000
# Production (pour EAS Secrets) : https://taptapgo.onrender.com
# EXPO_PUBLIC_BACKEND_URL_IOS=
# EXPO_PUBLIC_BACKEND_URL_NATIVE=
# EXPO_PUBLIC_BACKEND_URL_WEB=

# Mapbox (cartes) — en .env pour le dev ; pour EAS build, ajouter en secret EAS
# EXPO_PUBLIC_MAPBOX_TOKEN=pk.xxx

# Token téléchargement SDK Mapbox (build Android natif). Obligatoire pour npx expo run:android.
# Créer un token SECRET (sk.xxx) avec scope "Downloads: Read" : https://account.mapbox.com/access-tokens/
# Sinon : ajouter dans frontend/android/local.properties : MAPBOX_DOWNLOADS_TOKEN=sk.xxx
# RNMAPBOX_MAPS_DOWNLOAD_TOKEN=sk.xxx

# White-label (optionnel)
# EXPO_PUBLIC_BRAND_ID=
# EXPO_PUBLIC_BRAND_NAME=
