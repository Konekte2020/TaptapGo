/**
 * Mapbox a été retiré du projet. Ce module ne fait plus rien (no-op).
 * La navigation utilise Google Maps / liens externes.
 */
function getMapbox(): null {
  return null;
}

export const initMapbox = () => {};

export default getMapbox;
